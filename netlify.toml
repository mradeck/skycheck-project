[functions."zones-fr"]
  included_files = ["data/fr-zones-tiles/**/*"]

[functions."zones-at"]
  included_files = ["data/uas-zones-at.json"]

# Root-URL liefert die App ohne /skycheck.html-Suffix.
# Rewrite (Status 200) — die URL in der Adresszeile bleibt "/".
# Hostname-basierte COUNTRY-Detection in skycheck.html funktioniert weiter.
[[redirects]]
  from = "/"
  to = "/skycheck.html"
  status = 200

# Sicherheits-Header (Defense-in-Depth). Single-File-Inline-Script-PWA, daher ist
# script-src 'unsafe-inline' unvermeidbar; die Schutzwirkung gegen eingeschleuste
# Callsign-/Ortsnamen-/METAR-Payloads liefert das restriktive connect-src/img-src
# (keine Exfiltration an fremde Hosts). Zusammen mit escapeHtml doppelte Absicherung.
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Content-Security-Policy = "default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; script-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; style-src 'self' 'unsafe-inline' https://cdnjs.cloudflare.com; img-src 'self' data: blob: https://cdnjs.cloudflare.com https://*.basemaps.cartocdn.com https://*.tile.openstreetmap.org https://server.arcgisonline.com https://uas-betrieb.de https://wms.geo.admin.ch https://servais.enaire.es; connect-src 'self' https://api.airplanes.live https://api.brightsky.dev https://api.open-meteo.com https://photon.komoot.io https://uas-betrieb.de https://api.allorigins.win https://utm-service.dfs.de https://services.swpc.noaa.gov https://kp.gfz.de https://aviationweather.gov https://api3.geo.admin.ch https://servais.enaire.es https://services-eu1.arcgis.com; font-src 'self' data:; worker-src 'self'; manifest-src 'self'"

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# Die statischen OSM-Zusatzebenen sind groß und ändern sich nur bei einem
# bewusst erzeugten Datenupdate. CDN und Browser dürfen sie deshalb einen Tag
# direkt bedienen und danach im Hintergrund neu validieren.
[[headers]]
  for = "/data/*"
  [headers.values]
    Cache-Control = "public, max-age=86400, stale-while-revalidate=604800"

# Die AIRAC-Dateien ändern sich nur mit einem neuen DiPUL-Zyklus. Ein kurzer
# Browser-/CDN-Cache hält Wiederholungsaufrufe schnell, ohne einen neuen Stand
# länger als einen Tag zurückzuhalten.
[[headers]]
  for = "/data/dipul-airac/*"
  [headers.values]
    Cache-Control = "public, max-age=86400"
